name = "nowplaying-badge"
main = "src/index.js"
compatibility_date = "2025-07-18"
workers_dev = true

[vars]
# RELAY_URL 是本项目作者自己的 state-worker 部署地址——自建时必须换成你自己的
# state-worker 域名/workers.dev 地址,否则徽章会无错误地展示作者本人此刻在听的歌
# (代码里的默认值就是这个地址,详见 src/index.js 顶部注释和 README「从零搭建
# badge-worker」)。(此前误留了一个 badge-worker 代码里根本不读的 LB_USER,已移除。)
RELAY_URL = "https://np.yudaotor.me"
