/web/
/feishu-bot/
/bin/
/lyrimuse-collector/collector
.wrangler/
node_modules/
.DS_Store
.dev.vars
lyrimuse/.build/
lyrimuse/dist/
# 在仓库根跑 swift 命令时 SwiftPM 会在这里也建一个 .build
/.build/
__pycache__/
.playwright-mcp/

# AI 协作文件只留本地,不进仓库(2026-09-11 用户定)。
# 这三份 skill 与两个入口文件都是给 Claude / Codex 读的操作约束,对使用者和贡献者没有意义,
# 而且会随会话频繁改动。删的是「跟踪」不是文件本身——它们仍在工作树里正常生效。
# 连带改动:selftest 的「项目级 skill」守卫改成目录不存在就整段跳过(否则别人 clone 出来 CI 会红)。
/AGENTS.md
/CLAUDE.md
/.claude/
# 同类的其它 AI 工具入口,加在这里是为了"换个工具也不会又冒出来一个"
# (2026-09-11 加规则时这些路径都不存在、也没被跟踪,纯预防)。
/GEMINI.md
/.codex/
/.cursor/
/.cursorrules
/.windsurfrules
/.aider*
/.continue/
/.github/copilot-instructions.md

# 只留在本地、不进远端的内部文档(2026-09-15 用户拍板)。这些是过程性内容:功能现状
# 文档带着每条决策的实测记录与走过的弯路、发布流程、hero 图的素材与提案草稿 ——
# 对自己有用,但对着公开仓的读者既不友好也没必要。**文件仍然留在磁盘上**,只是不再
# 跟踪;对外的那几份(README 的截图 docs/images/、scrobbling 说明、LyricsX 对比页)
# 刻意不在此列。
# ⚠️ 历史里的旧版本没有清除(仓是 public,翻旧 commit 仍读得到)——当时选的是"只管
# 以后",不重写历史。
/docs/features/
/docs/proposals/
/docs/heroes/
/docs/releasing.md

# 本地素材/文案工作目录(2026-09-16 起常驻此处,用户拍板)。搬进仓里的原因是桌面目录
# 受 macOS TCC 保护、工具读不到,放这儿 Claude 才能直接读写。里面是对外文案草稿、
# 设计稿、Last.fm 数据导出、诊断文件,60MB / 200+ 文件,**一件都不该进这个公开仓** ——
# 它就是要"留在本地"的东西,跟上面那几份内部文档同一个立场。
/Lyrimuse资料/
