# 允许存在的不可达函数（`deadcode -test ./...` 的输出，只写函数名、不写行号）。
#
# CI 只在出现**这份清单之外**的新死函数时失败。闸的目的不是"零死代码"，是挡住
# 「函数已经没人调用、解释它的注释却还活着」这种会误导读者的组合 —— 仓里踩过：
# 一段描述已删除机制的注释，让后来的判断顺着它整个反了方向。
#
# 往这里加一条之前先问：这个函数的名字是不是已经成了别处注释描述某条策略时用的词？
# 是就留（改名的代价是改十几处讲策略的注释，不划算）；不是就删掉它，别加进来。
#
# 清理掉某一条之后，把它从这份清单里删掉 —— CI 会提示，但不会因此失败。

# 这两个是 Detailed 版本的薄包装，调用方都已经改用 Detailed 版（要拿时长误差做裁决）。
# 保留的唯一理由：这两个名字是本仓描述这两条兜底策略的通用词汇，netease.go / enrich.go /
# searchcli.go 里共有二十来处注释拿它们讲判据和真实案例（方大同「Love Love Love」、
# Khalil Fong《Revisited》两案）。删函数就要改那二十来处注释，而那些注释讲的是策略、
# 不是这两行代码。
retryTitleFromAlbum
retryTitleFromArtistSearch
